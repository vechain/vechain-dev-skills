# Promptfoo evaluation config for VeChain dev skills
# See: https://promptfoo.dev/docs/configuration/reference

description: VeChain Dev Skill Evaluation

providers:
  - id: file://scripts/anthropic-provider.ts
    label: claude-sonnet

defaultTest:
  options:
    provider:
      config:
        temperature: 0

# Import test suites
tests: suites/**/*.yaml
